# 全局规则

## 语言
- 回复使用中文。

## Python 环境
- 当需要在终端中使用 Python 时，先激活 Conda 环境：
  ```
  conda activate forskills
  ```
- 然后在激活的环境中运行 Python 命令，不要使用系统默认的 Python。
- 与 skill 相关的 Python 使用，不要直接调用系统默认的 Python。

## Skill 优先级
- 当用户的任务匹配到可用 skill 时，优先使用对应的 skill。
